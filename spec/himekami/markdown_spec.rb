require "spec_helper"

RSpec.describe Himekami::Markdown do
  it "has a version number" do
    expect(Himekami::Markdown::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
